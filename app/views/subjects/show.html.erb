<div class="container">
   <table class="table table-hover">
     <thead class="thead-light">
       <tr>
         <th>Titel</th>
       </tr>
     </thead>
     <tbody>
       <% @subject.books.each do |b| %>
         <tr>
           <td><%= link_to b.title, show_book_path(b.id) %></td>
         </tr>
       <% end %>
     </tbody>
   </table>
  <%= link_to "Subjectliste", subjects_url%>
</div>
